package main;

/**
 * Class that shows all the positions that "Bob" can go through in the maze
 *
 * @param Positions   Declares positions traversed in the maze
 * @return            void 
 */

public enum Positions {
	TOP,
	BOTTOM,
	LEFT,
	RIGHT
}
